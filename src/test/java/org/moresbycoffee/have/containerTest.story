Scenarion simple container test
When a blah blah is added to a container called: testContainer
Then the testContainer container should contain the blah blah

When a secret message is added to a container called: envelop
Then the envelop container should contain the secret message

Scenario what checks the parameter type matching
When an integer added to a container called integerContainer
Then the integerContainer (containing the integer) can be retrieved as number container
